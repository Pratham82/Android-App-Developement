fun main(){
  //readLine() will take the user input and always convert it to string
  var name = readLine()
  print("Hello, Good morning ${name}")

  //Converting the value of the input from string to Int
  var age =  Integer.valueOf(readLine())
  println(age)
}
