// Main function will run first in any programm
fun main(){
  println(add(45,45))
}

//User defined functions
// We also have to pass the types of variables as well as retrun type of the function
fun add(num1:Int, num2: Int):Int{
  return num1 +num2
}
